# Исправленные ошибки

## Список исправлений:

1. **tsconfig.json** - Исправлена устаревшая конфигурация TypeScript
   - Удалена устаревшая опция `baseUrl` 
   - Настроена правильная структура с references на tsconfig.app.json и tsconfig.node.json

2. **Установлены недостающие зависимости**
   - Добавлена библиотека `react-window` и `@types/react-window`

3. **src/utils/formuls.ts** - Исправлены неиспользуемые переменные
   - Удалены неиспользуемые параметры `start` и `end` в функции replace
   - Удалена неиспользуемая переменная `e` в блоке catch

4. **src/components/Cell.tsx** - Унифицированы типы
   - Удалены дублирующиеся определения типов
   - Импортированы типы из централизованного файла `types/type.ts`
   - Исправлена сигнатура функции `onSelect` (убран параметр `shiftKey`)

5. **src/components/table.tsx** - Переписан компонент таблицы
   - Заменен импорт react-window на простую CSS Grid реализацию
   - Исправлены импорты (убраны расширения .js)
   - Добавлены правильные типы для пропсов
   - Использован useCallback для оптимизации

6. **src/types/types.ts** - Удален дублирующийся файл
   - Оставлен только один файл типов `type.ts`

7. **src/App.tsx** - Обновлен главный компонент
   - Заменен шаблонный код на использование компонента Spreadsheet
   - Добавлен заголовок "Таблица"

## Результат:
- ✅ Проект успешно собирается (`npm run build`)
- ✅ Линтер не выдает ошибок (`npm run lint`)
- ✅ Все TypeScript ошибки исправлены
- ✅ Код соответствует стандартам ESLint

## Запуск проекта:
```bash
npm install
npm run dev
```
